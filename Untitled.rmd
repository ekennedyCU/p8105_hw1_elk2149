---
title: "Homework 1"
output: github_document
date: "2025-09-18"
---

```{r problem_1_setup}
library(moderndive)
library(tidyverse)
data("early_january_weather")
help("early_january_weather")
```

The variables in this dataset are: `r (names(early_january_weather))`.

Flights were recorded from `r unique(early_january_weather $ year)`. The average temperature was `r mean(early_january_weather$temp)` °F.

The dataset has `r nrow(early_january_weather)` rows and `r ncol(early_january_weather)` columns.

```{r tempXtime_hour_scatter}
ggplot(early_january_weather, aes(x = time_hour, y = temp)) + 
    geom_point()
```
